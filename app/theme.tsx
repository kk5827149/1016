'use client';
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    mode: 'light',
    primary: {
      main: '#3be460',
    },
    secondary: {
      main: '#f50057',
    },
  },
});

export default theme;
